import React from "react"
import Grid from '@mui/material/Grid';

export default function SideBarPost(){

    return (
        <Grid item xs={4} class="sideBarPost">
            <span class="title">Autres liens utiles</span>
        </Grid>
    );
}